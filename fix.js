const fs = require('fs'); const file = 'd:/projects/apckarma/app/admin/page.js'; let content = fs.readFileSync(file, 'utf8'); content = content.replace('const [logs, setLogs] = useState([]);', 'const [logs, setLogs] = useState(() => { if (typeof window !== \'undefined\') { const cached = localStorage.getItem(\'apckarma-admin-logs\'); if (cached) return JSON.parse(cached); } return []; });').replace('const [loading, setLoading] = useState(true);', 'const [loading, setLoading] = useState(false);').replace('setLogs(logsRes);', 'setLogs(logsRes); localStorage.setItem(\'apckarma-admin-logs\', JSON.stringify(logsRes));'); fs.writeFileSync(file, content);
